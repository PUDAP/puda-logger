[project]
name = "puda-logger-tests"
version = "0.1.0"
description = "Connection and integration tests for puda-logger"
requires-python = ">=3.12"
dependencies = [
    "psycopg[binary]>=3.2",
]

[tool.uv]
dev-dependencies = []
