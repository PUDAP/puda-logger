[project]
name = "influxdb-logger"
version = "0.1.0"
description = "Logger service that streams PUDA NATS machine traffic and writes to InfluxDB"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "influxdb3-python>=0.8.0",
    "nats-py>=2.14.0",
]
