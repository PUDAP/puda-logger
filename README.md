# puda-logger

Services for logging PUDA machine traffic:

- `influxdb/` — InfluxDB server and NATS → InfluxDB logger
- `postgres/` — PostgreSQL server and NATS → PostgreSQL logger
- `livestream/` — MediaMTX livestream relay
